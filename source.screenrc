# use z as the escape key
escape ^Zz
# we don't use a login shell, because I want the env to be nicely automatically
# bootstrapped for us.But I'm saving the config here anyways for reference:
# shell -$SHELL

autodetach on
# show the windows at the bottom
hardstatus alwayslastline "%w"

# lots of history; why not.
defscrollback 10000 
startup_message off
